#include <stdio.h>

#include "cstring.h"

// TODO, stress test the program and fix bugs

int main() {
  string * arr[10];

  for (int i = 0; i < 10; i++) {
    arr[i] = cstring(NULL);
  }

  for (int i = 0; i < 10; i++) {
    arr[i] = cstring(NULL);
  }
}
